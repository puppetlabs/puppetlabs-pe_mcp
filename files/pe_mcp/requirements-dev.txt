pytest>=8.0.0
pytest-bdd>=7.0.0
pytest-asyncio>=0.23.0

# semgrep intentionally NOT listed here: it hard-pins mcp==1.23.3, which lacks
# streamable_http_client and breaks fastmcp's client import if installed
# alongside these test deps. CI installs semgrep in its own isolated job/env
# (see .github/workflows/python-checks.yml); do the same locally: `pip install semgrep`.
